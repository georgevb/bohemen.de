---
// Lightbox component for image viewing with keyboard navigation
---

<div id="lightbox" class="lightbox hidden" role="dialog" aria-modal="true" aria-label="Bildansicht">
  <button 
    id="lightbox-close" 
    class="absolute top-4 right-4 text-white text-4xl z-10 hover:text-gray-300 transition-colors"
    aria-label="Schließen"
  >
    &times;
  </button>
  
  <button 
    id="lightbox-prev" 
    class="absolute left-4 top-1/2 -translate-y-1/2 text-white text-4xl hover:text-gray-300 transition-colors"
    aria-label="Vorheriges Bild"
  >
    ‹
  </button>
  
  <button 
    id="lightbox-next" 
    class="absolute right-4 top-1/2 -translate-y-1/2 text-white text-4xl hover:text-gray-300 transition-colors"
    aria-label="Nächstes Bild"
  >
    ›
  </button>
  
  <div class="flex items-center justify-center h-full p-8">
    <img 
      id="lightbox-image" 
      src="" 
      alt="" 
      class="max-w-full max-h-full object-contain"
      loading="lazy"
    />
  </div>
  
  <div class="absolute bottom-4 left-1/2 -translate-x-1/2 text-white text-sm">
    <span id="lightbox-counter"></span>
  </div>
</div>

<style>
  .lightbox {
    @apply fixed inset-0 z-50 bg-black bg-opacity-95;
    backdrop-filter: blur(10px);
  }
</style>